export enum WithdrawalMethod {
  Payoneer = "payoneer",
  BankTransfer = "bankTransfer",
}

export enum ContractType {
  Hourly = "hourly",
  Fixed = "fixed",
}

export enum ContractFees {
  Ten = 0.10,
  Five = 0.05,
  Three = 0.03,
  Zero = 0.00,
}